[project]
name = "{{name}}"
version = "1.0.0"
description = "{{description}}"
requires-python = ">=3.12"

[project.scripts]
{{name}} = "{{cli_name}}.cli:main"

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
